export type User = {
  id: string;
  displayName?: string;
  jobTitle?: string;
  department?: string;
  createdDateTime?: string;
  accountEnabled?: boolean;
  onPremisesSyncEnabled?: boolean;
  preferredLanguage?: string;
  role?: string;
};
